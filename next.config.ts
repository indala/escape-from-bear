import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['priest-southwest-valuable-invite.trycloudflare.com'],
};

export default nextConfig;
